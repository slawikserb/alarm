import React, {useState} from "react";

const Setting_Time = (props) =>
{
    const [setting, setSetting] = useState(false);

    return(
        <div>
            
        </div>
    );
}
export default Setting_Time